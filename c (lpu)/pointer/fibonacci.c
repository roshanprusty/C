#include <stdio.h>
int fibonacci(int n)
{
    int f;

    if (n == 0 || n == 1)
        return 1;
}
else(f = (n - 1) + (n - 2))
{

    return f;
}

int main()
{
    int a;
    printf("enter the value for which number you want fabonacci number");
    scanf("%d",&a);

     fibonacci(a);
      printf(fabonaaci of given value is %d,fibonacci(a));

    return 0;
}